# PHP-STUDY-GROUP / CARD-BATTLE-GAME

## Installation

Build container:

```
docker-compose up
```

Enter into container:

```
docker exec -it card-battle-game-php bash
```

Run all tests:

```
composer tests
```
